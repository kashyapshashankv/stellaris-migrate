# permissions for end users to edit stellaris-migrate-nodes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: migration
    app.kubernetes.io/managed-by: kustomize
  name: stellarismigratenode-editor-role
rules:
- apiGroups:
  - migrate.k8s.stellaris.io
  resources:
  - stellaris-migrate-nodes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - migrate.k8s.stellaris.io
  resources:
  - stellaris-migrate-nodes/status
  verbs:
  - get
